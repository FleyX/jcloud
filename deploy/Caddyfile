:{$JCLOUD_PORT:80} {
    root * /srv/web
    encode gzip zstd

    # API 请求透传给后端，保留 /jcloud/api 前缀
    handle /jcloud/api/* {
        reverse_proxy localhost:{$JCLOUD_APP_PORT:8080}
    }

    # 其余请求交给前端 SPA
    handle {
        try_files {path} {path}/ /index.html
        file_server
    }
}

# JCloud 上下文

JCloud 是一个前后端分离的个人网盘。本文档记录项目核心领域的术语表。

## 术语表

**PC 端（Desktop）**：
通过 User-Agent 判定为桌面浏览器（含平板）的设备访问渠道。在此渠道下，应用采用顶部一级导航栏 + 左侧二级边栏 + 右侧主内容区的分层布局。

**移动端（Mobile）**：
通过 User-Agent 判定为手机等移动设备的访问渠道。在此渠道下，应用采用底部 TabBar + 固定顶部栏 + 主内容区的布局，各页面使用独立的移动端视图组件重新实现。

**设备外壳（Device Shell）**：
包裹非公开路由的根级布局组件，分为 `PcAppShell` 与 `MobileAppShell`。`App.vue` 根据当前设备类型选择渲染哪一套外壳。

**视图代理（Device View Resolver）**：
路由配置中的组件工厂函数，根据当前设备类型动态加载 `views/pc/` 或 `views/mobile/` 下的同名视图文件。它保证路由表只有一份，route name 不变。

**公开路由（Public Route）**：
无需登录即可访问的页面（如登录、注册）。公开路由不经过设备外壳，直接由 `App.vue` 渲染 `router-view`。

**一级模块（Primary Module）**：
全站核心功能域的顶层划分，对应 PC 端顶部导航栏与移动端底部 TabBar 的条目。示例：`files`（文件）、`system`（系统）。一级模块由 `menuStore` 统一管理。

**二级菜单（Secondary Menu）**：
某个一级模块下的具体页面入口，对应 PC 端左侧边栏的菜单项，以及移动端顶部栏抽屉中的列表项。示例：`/admin/users`、`/admin/roles`、`/admin/storage-spaces` 均属于 `system` 模块的二级菜单。

**移动顶部栏（Mobile Header）**：
移动端设备外壳中的固定顶部栏，承载品牌图标、当前一级模块标题（点击展开二级菜单抽屉）以及用户头像入口。

**移动侧边抽屉（Mobile Drawer）**：
移动端从屏幕左侧滑出的抽屉面板，展示当前一级模块下的全部二级菜单，用于解决底部 TabBar 无法承载二级入口的问题。

**个人中心页（Profile Page）**：
展示当前登录用户信息的顶层页面，移动端通过点击顶部栏头像进入，独立于任何一级模块之外。

## 文件管理领域

**文件节点（FileNode）**：
文件与文件夹在数据库中的统一实体，通过 `type: 'file' | 'folder'` 区分。所有节点共享同一套树形结构字段（`parent_id`、`path`）。

**文件（File）**：
`type = 'file'` 的文件节点，代表用户上传的二进制对象。同一父目录下文件名唯一。

**文件夹（Folder）**：
`type = 'folder'` 的文件节点，用于组织其他文件节点，可嵌套。文件夹本身是虚拟的，不直接对应物理目录；物理目录在文件写入时由 `存储空间路径 / files / username / 虚拟路径` 推导得到。

**物化路径（Materialized Path）**：
文件节点祖先 ID 的快照，**不包含自身 ID**，使用 `.` 分隔的 13 位 base36 id 表示，如 `rootId.parentId`。用于快速定位子树与判断层级关系。虚拟根节点使用固定占位 id `0000000000000`。

**虚拟文件树（Virtual File Tree）**：
每个用户拥有的一棵独立逻辑目录树，由 FileNode 构成。

**存储空间（Storage Space）**：
管理员维护的后端存储目录（如 `/data`、`/data1`）。用于存放用户文件数据，每个用户绑定到一个用户存储空间。

**用户存储空间（User Storage Space）**：
存放用户文件数据的存储空间。每个用户绑定到一个用户存储空间；该用户的所有文件字节均落在该空间内，受用户配额与存储空间容量双重限制。

**用户默认存储空间**：
创建用户时指定的用户存储空间，该用户所有文件均存放于此。

**系统数据目录（System Data Directory）**：
管理员在任意一个已有的用户存储空间中指定的子目录（固定为 `<存储空间路径>/system`），用于存放系统级数据，如预览图、ZIP 临时文件、系统配置文件等。系统数据不计入用户配额，容量由管理员监控所属存储空间。

**用户编码（User Code）**：
用户的登录名，即 `t_user.username`，用于在存储空间下隔离不同用户数据的目录名，是物理路径的固定组成部分。username 全局唯一，且不可修改。

**物理路径（Physical Path）**：
文件字节的实际磁盘位置，由 `存储空间路径 / files / username / 名称路径` 推导得到。名称路径通过物化路径（id 路径）查询所有祖先节点的 `name` 实时拼接，批量处理时缓存已查询的 `id -> name` 映射。

**用户配额（User Quota）**：
单个用户可使用的最大存储容量，由管理员配置。

**存储空间容量（Storage Space Capacity）**：
一个存储空间允许存放的所有用户数据总量上限。

**已用空间（Used Space）**：
用户本地文件节点大小之和，文件上传完成后实时累加；删除文件时相应扣减。同一用户内的去重文件（硬链接）只计算一次物理大小。`source_type = 'remote'` 的远程文件节点不计入已用空间。

**空间不足**：
上传操作同时检查用户配额与存储空间容量，任一达到上限即拒绝上传，并提示用户联系管理员。远程文件节点不参与本地空间不足判断。

**分片上传（Chunked Upload）**：
文件按默认 64MB 大小（可通过 `jcloud.upload.chunk-size` 配置，范围 1MB~512MB）切分为多个分片上传，每个分片独立校验 hash，支持断点续传。

**内容标识符（Content Identifier）**：
`t_file_node.hash` 字段存储的文件级标识。`source_type = 'local'` 时为文件身份 Hash，用于秒传去重；`source_type = 'remote'` 时为远端存储返回的 etag，用于增量同步 diff。

**文件身份 Hash（File Identity Hash）**：
本地文件的内容标识符计算方式。文件小于 150MB 时计算完整 hash；文件大于等于 150MB 时取前 50MB、中间 50MB（从 `floor(size/2)` 开始）、后 50MB 拼接后计算 hash。

**秒传（Instant Upload）**：
用户上传本地文件前，服务端发现该用户已存在相同文件身份 Hash 的文件，则直接创建新的 FileNode 并复用已有物理文件，无需重新上传字节。复用优先使用硬链接；若硬链接失败（如跨文件系统），则退化为普通文件复制。远程文件节点不参与秒传。

**用户内去重（Per-User Deduplication）**：
秒传只在同一用户范围内查找重复文件，不同用户之间不共享物理 inode，避免配额与迁移的复杂性。

**存储空间扩容**：
管理员扩大当前存储空间可用容量的操作，用于解决该空间上用户的空间不足问题。

**用户文件迁移**：
管理员将某用户的全部文件从一个存储空间物理迁移到另一个存储空间的操作。

**回收站（Recycle Bin）**：
每个用户拥有独立的虚拟回收站目录。删除文件/文件夹时，FileNode 被移入回收站并记录原始路径，物理文件原位保留以便恢复。

**保留期（Retention Period）**：
回收站中文件以及注销后用户数据的自动彻底删除等待时间，当前为 7 天。

**彻底删除（Permanent Delete）**：
从数据库和物理磁盘上移除文件或删除记录，释放配额与存储空间。`t_file_node` 和 `t_recycle_bin` 均使用物理删除。

**上传冲突处理（Upload Conflict Handling）**：
上传文件时，若目标位置已存在同名文件或同名文件夹，后端将冲突列表返回给前端。用户可选择对冲突项执行“跳过”“覆盖”或“自动重命名”；自动重命名格式为 `report.1.pdf`。批量上传时支持“全部应用”选择。

**文件夹上传（Folder Upload）**：
通过浏览器原生 `<input webkitdirectory>` 选择本地文件夹后上传其全部内容，并保持原有层级结构。后端负责根据相对路径自动创建文件夹层级。

**相对路径（Relative Path）**：
文件夹上传时文件相对于当前目录的层级路径，使用 `/` 分隔，包含文件名，例如 `project/src/main.java`。其目录部分用于后端创建文件夹结构，文件名部分用于最终文件命名。

**两阶段冲突解决（Two-Phase Conflict Resolution）**：
上传、恢复、移动、重命名、复制等操作分为预检阶段与执行阶段。预检阶段后端返回冲突列表并获取该用户的 Redisson 写锁；用户针对每项选择“跳过”“覆盖”或“自动重命名”后，进入执行阶段按策略处理。写锁设有有效期（如 30 秒），超时未选择则锁自动释放，前端需重新预检并提示用户在限定时间内完成选择。

**恢复（Restore）**：
将回收站中的文件/文件夹还原到其原始路径；若原始路径中的父目录已不存在，则自动创建缺失的文件夹。若原始路径已存在同名文件/文件夹，后端返回冲突列表，由用户选择“跳过”“覆盖”或“自动重命名”。

**移动/重命名（Move/Rename）**：
变更文件或文件夹的 `parent_id` 或 `name` 的操作。重命名只更新自身 `name`，不再级联更新子树；移动文件夹时需要批量更新子树的 `path`。若目标路径已存在同名文件/文件夹，使用两阶段冲突解决，用户可选择“跳过”“覆盖”或“自动重命名”。

**复制（Copy）**：
创建源文件/文件夹的副本到目标路径的操作。若目标路径已存在同名文件/文件夹，使用两阶段冲突解决，用户可选择“跳过”“覆盖”或“自动重命名”。

**写操作（Write Operation）**：
上传、删除、移动、重命名、恢复、清空回收站等会改变文件树或物理存储的状态变更操作。

**用户级读写锁（Per-User Read-Write Lock）**：
为每个用户分配的并发控制锁。同一用户的多个读操作可并发执行，但同一时刻只允许一个写操作执行。

**删除记录（Recycle Record）**：
`t_recycle_bin` 表中的一条记录，对应一次用户主动发起的删除行为。它只记录被直接勾选的文件或文件夹节点，不展开记录其所有后代。该表使用物理删除。

**回收站物理目录（Recycle Physical Directory）**：
被删除节点在物理磁盘上的临时存放位置，路径为 `存储空间路径 / trash / username / <删除记录 ID> /`。删除记录 ID 为 13 位 base36 字符串，目录名直接使用该字符串。文件夹的子树结构在该目录下保持原样，文件的元数据在恢复时重新计算。

**临时文件物理目录（Temporary File Directory）**：
分片上传等过程中产生的临时数据存放位置，路径为 `存储空间路径 / tmp / username / <上传任务 ID> /`。

**文件管理核心模块**：
JCloud 中负责文件与文件夹的创建、读取、更新、删除的基础能力集合，当前包括上传、下载、浏览、组织、删除、元信息/预览，不包括分享、协作编辑、版本历史。

**Office 文档预览（Office Preview）**：
将 `doc`/`docx`/`xls`/`xlsx`/`ppt`/`pptx` 通过 LibreOffice 同步转换为 PDF 供前端 pdf.js 渲染的预览能力，预览类型为 `office`，产物按现有机制缓存于系统数据目录。`pdf` 原文件不转换、不缓存，直接流式返回。受三道闸门限制：文件大小上限（默认 50MB）、单次转换超时（默认 60 秒）、全局并发转换数（默认 2，每次转换使用独立 soffice user profile），均可通过 `jcloud.preview.*` 配置。

## 远程资源挂载领域

**远程挂载点（Remote Mount Point）**：
用户虚拟文件树中代表远端存储资源的入口文件夹，首期仅允许作为用户虚拟文件树的一级目录（即直接挂在根节点下）。它是用户可见的 FileNode，`source_type = 'remote'` 并拥有独立的挂载点级操作语义：删除映射为取消挂载（不删除远端数据），重命名只更新本地显示名与配置名，不操作远端根目录。

**远程资源配置（Remote Storage Config）**：
描述如何连接一个远端存储后端的配置对象，与用户绑定。通用属性（如名称、协议类型、定时同步表达式、启用状态）平铺存储；协议特有参数（如 URL、用户名、密码、根路径）以 JSON 形式存放，方便后续 S3、NFS 扩展。凭据字段需加密存储。

**协议配置（Protocol Config）**：
远程资源配置中随协议类型变化的连接参数集合，如 WebDAV 的 URL、用户名/密码、远端根路径；S3 的 bucket、region、accessKey 等。

**远程协议适配器（Remote Protocol Adapter）**：
策略模式中的具体协议实现，封装对远端存储的底层操作（如 list、download、upload、delete、move、createFolder）。首期实现 WebDAV 适配器，后续通过新增适配器支持 S3、NFS 等协议，无需改动上层文件操作逻辑。

**远程挂载管理（Remote Mount Management）**：
用户管理自己远程挂载配置的入口，位于 `files` 一级模块下的二级菜单。用户可在此新增、编辑、删除、手动同步、配置定时同步远程挂载点。

**凭据加密（Credential Encryption）**：
对远程资源配置中的敏感信息（如密码、accessKey）进行对称加密存储的机制；解密密钥由服务端配置持有，不落入数据库。

**同步（Sync）**：
将远端存储的目录结构与元数据全量拉取并映射为本地 FileNode 的过程，使用户可以在 jcloud 中搜索、浏览远程内容。同步不实时发生，由用户手动触发或按策略自动触发。

**同步任务（Sync Task）**：
一次同步操作的执行单元，负责拉取远端元数据、比对本地 FileNode，并执行增删改以保持一致。首次同步时本地镜像为空，等价于一次全量同步；后续同步基于远端元数据做增量 diff。

**手动同步（Manual Sync）**：
用户在远程挂载管理界面主动触发的一次同步任务。

**创建后同步（Post-Creation Sync）**：
创建远程挂载点后触发的首次同步。该同步不在创建接口内同步执行，而是通过调用同步接口异步触发，避免创建接口被远程存储调用拖慢或阻塞。

**定时同步（Scheduled Sync）**：
按每个远程挂载点独立配置的 cron 表达式周期性触发的同步任务。

**远程同步任务记录（Remote Sync Task Record）**：
记录每一次远程挂载同步执行情况的持久化对象，包含触发方式、状态、起止时间、同步节点总数/成功数/失败数及错误信息，便于用户排查和历史审计。

**来源类型（Source Type）**：
文件节点的来源分类，取值为 `local`（本地存储空间）或 `remote`（远程挂载）。用于区分文件字节是否落在本地盘、是否参与本地配额与容量统计。

**远程文件节点（Remote FileNode）**：
`source_type = 'remote'` 的文件节点，其内容由远端存储提供，`remote_mount_id` 指向对应的远程挂载配置。远程文件节点可能具有与本地文件节点不同的可操作集合（如下载、预览）。

**远程预览缓存（Remote Preview Cache）**：
为远程文件生成的预览图（缩略图）在系统数据目录中的缓存。下载远程文件本身不在服务器缓存，但预览图可缓存以提升重复预览性能。缓存路径包含远程文件的内容标识符（etag），内容变更后自动失效；同步任务检测 etag 变化时主动清理旧缓存。

**本地文件节点（Local FileNode）**：
`source_type = 'local'` 的文件节点，文件字节落在本地用户存储空间内，受用户配额与存储空间容量限制。

**远程写回（Remote Write-Back）**：
用户在 jcloud 中对远程文件节点发起的写操作（如删除、重命名、移动、上传）会实时回写到远端存储后端。远端调用失败时，本地 FileNode 的变更立即回滚，保持本地与远端一致。

**冲突策略（Conflict Policy）**：
同步或写回过程中本地状态与远端状态发生冲突时的裁决规则。当前规则为「以远程为准」，即远端状态覆盖本地状态。

**混合目录写操作（Mixed Directory Write）**：
同时涉及本地目录与远程目录的批量写操作（如一次性文件夹上传覆盖本地与远程目录），首期直接拒绝并提示用户，避免跨来源的复杂事务。

**跨来源写操作（Cross-Source Write）**：
源路径与目标路径分属不同来源的写操作，包括本地↔远程、以及两个不同远程挂载点之间的复制与移动。同来源内部的写操作仍走原有路径；跨来源复制/移动通过「跨来源传输」完成。

**跨来源传输（Cross-Source Transfer）**：
跨来源复制/移动的执行机制。预检阶段同步完成（复用两阶段冲突解决，远程目标的同名冲突基于本地镜像检测，执行时复查远端）；执行阶段为异步任务，逐文件将字节经服务器中转（源端下载 → 目标端上传），支持文件夹递归。远程→本地传输仅校验用户配额与存储空间容量，不参与秒传去重；本地→远程传输写回成功后立即创建本地 FileNode 镜像。传输任务支持用户取消：当前文件传完后停止，已传输部分保留。

**跨来源移动（Cross-Source Move）**：
跨来源传输中的移动操作，语义为「复制成功 + 删除源」，与同来源移动（仅改元数据）本质不同。以单文件为原子单元：该文件复制成功才删除其源；删除源遵循各来源现有删除语义（本地源进回收站，远程源按远程写回真删）。部分文件失败时其余继续，任务结果记为成功/部分成功/失败并记录明细。

**传输任务（Transfer Task）**：
一次跨来源传输的持久化执行单元，记录来源/目标类型、操作类型（复制/移动）、状态、节点总数/成功数/失败数、失败明细与起止时间。预检阶段持用户级读写锁，执行阶段按单文件获取短锁，涉及远程挂载点时持挂载点级锁。前端复用现有复制/移动入口发起，通过轮询任务状态展示进度。

**远程上传（Remote Upload）**：
将本地文件或文件夹上传到远程目录的写操作。该操作不走本地秒传/去重流程，文件字节直接通过协议适配器写入远端存储。

**远程新建文件夹（Remote Folder Creation）**：
在远程目录内创建文件夹的写操作，通过协议适配器在远端存储创建对应目录（如 WebDAV 的 `MKCOL`），同时在本地 FileNode 中创建记录。

**挂载点级锁（Mount-Level Lock）**：
按远程挂载点分配的分布式锁，用于串行化该挂载点上的同步任务与远程写回操作，避免并发冲突与重复处理。

**取消挂载（Unmount）**：
用户删除远程挂载配置的操作。取消挂载会级联删除该挂载点下所有 `source_type = 'remote'` 的本地 FileNode 镜像，但不删除远端存储中的实际数据。

**挂载点状态检查（Mount Health Check）**：
系统每天首次被用户访问时触发的远程挂载点可用性检查。发现连接异常或凭据失效的挂载点时，前端向用户展示提示，由用户手动处理；不自动禁用挂载点。

## WebDAV 访问领域

**WebDAV 访问（WebDAV Access）**：
登录用户通过 WebDAV 协议访问自己虚拟文件树的能力。JCloud 作为 WebDAV 服务端运行，用户在个人中心开启后，可通过 `/dav/{userCode}` 路径使用第三方 WebDAV 客户端挂载自己的整个文件树。

**WebDAV 服务端点（WebDAV Server Endpoint）**：
JCloud 对外暴露的 WebDAV 服务入口，统一前缀为 `/dav/{userCode}`，复用现有 Spring Boot 8080 端口。`{userCode}` 为用户编码（User Code），路径下直接映射该用户的虚拟文件树根目录。

**WebDAV 访问开关（WebDAV Access Toggle）**：
位于用户个人中心的布尔选项，默认关闭。用户手动开启后，即可使用主密码通过 WebDAV 协议访问自己的文件；关闭后服务端立即拒绝该用户的 WebDAV 认证。

**WebDAV 访问密码（WebDAV Access Password）**：
用户使用 WebDAV 协议登录时提供的密码。本期决策为与用户主密码相同，不在服务端单独存储或管理。

**WebDAV 写操作（WebDAV Write Operation）**：
通过 WebDAV 协议发起的上传、删除、移动、复制、创建文件夹等状态变更操作。写操作受用户级读写锁保护，遵循现有同名覆盖、物理删除等规则；COPY/MOVE 跨来源时与前端入口一致，走跨来源传输。

## 视频媒体库领域

**媒体库（Media Library）**：
用户创建的媒体内容集合，对应影视首页「我的媒体」中的一张库卡片。每个媒体库需指定媒体类型（电影、电视、其他），媒体类型创建后不可修改，填错只能删除重建。一个媒体库包含一个或多个来源目录，支持手动刷新与定时重扫（cron，定时任务只触发扫描不触发削刮）。扫描状态含 `SCANNING`（扫描中，服务重启后自动重置为失败）；增删来源目录会中断当前任务并强制全量重扫，被移除来源目录下的条目数据清空，播放进度随条目保留或清除。

**来源目录（Source Directory）**：
媒体库内用户从自己虚拟文件树中勾选的文件夹（本地或远程挂载点下均可），作为该媒体库的内容来源。一个媒体库可有多个来源目录；同一文件夹同一时刻只能属于一个媒体库（保存时跨库查重校验），同一媒体库内的来源目录之间不允许存在祖先/后代重叠。

**媒体类型（Media Type）**：
媒体库的分类，取值 `movie`（电影）、`tv`（电视）、`other`（其他）。电影/电视库的内容需从 TMDB 获取元数据并以海报墙展示；其他类型不获取元数据，以 ffmpeg 截图缩略图的网格卡片展示。电影/电视/其他海报墙均为分页加载（滚动到底加载下一页），支持关键词搜索（文件名/剧名/元数据标题/简介）与排序（添加时间/发行时间，升降序，默认添加时间降序）。

**媒体条目（Media Item）**：
媒体库扫描出的单个视频文件在媒体库中的记录，关联一个 FileNode。条目类型为 `movie`（电影）、`episode`（剧集）或 `other`。扫描时用 ffprobe 探测封装、视频/音频编码、时长（远程文件也流式探测），并记录匹配状态（已匹配/未匹配/手动修正）。

**文件变更哈希（File Change Hash）**：
`t_media_item.file_hash` 字段，扫描时对 `来源目录 ID + 相对路径（相对来源目录）+ 文件名 + 文件大小` 拼串计算的哈希值。增量扫描以此判断条目是否变化：不一致则重走完整解析流程（重解析剧/季归属 + 重跑 ffprobe），播放进度保留；`manual` 匹配状态保留，非 manual 重置为未匹配待削刮。

**扫描（Media Scan）**：
只负责文件事实的操作：遍历媒体库各来源目录子树、按文件变更哈希 diff 存量条目、ffprobe 探测、维护剧/季归属、清理消失文件的条目。扫描不访问 TMDB，不触碰元数据匹配（除上述重置外）。

**削刮（Metadata Scrape）**：
独立于扫描的元数据匹配操作，用户显式手动触发，默认只处理目录下未匹配的条目/剧（可选强制重新削刮所有非 manual 条目）。与扫描按目录互斥：同一目录同一时间只能跑扫描或削刮之一（内存任务锁，服务重启自然释放），不同目录可并行。电视以剧为单位削刮（一次匹配应用到全剧非 manual 集）；电影以条目为单位，先文件名后父目录名兜底。削刮状态（时间/结果/错误）持久化于目录表。削刮为**本地优先**：视频同目录存在 NFO 文件或本地媒体图片时完全信任本地内容、不请求 TMDB 补全（仅有本地图片无 NFO 时文本字段缺失不补、标记不完整）；本地缺失时才走 TMDB 匹配。削刮成功（含手动修正）后立即把元数据写回视频所在目录的 NFO 与图片（远程挂载来源经远程写回通道落盘），local_nfo 来源的内容为用户提供、已在视频目录，跳过写回；落盘失败不影响元数据入库，仅记录待重试。

**NFO 文件（NFO）**：
与视频同目录的 Jellyfin 兼容 XML 元数据文件：电影/集为与视频同名的 `.nfo`，剧文件夹为 `tvshow.nfo`。削刮成功后由 jcloud 自动写回（local_nfo 来源不写回，内容为用户提供、已在视频目录），也可由 Jellyfin 等外部工具产生。nfo/图片写入后作为普通 FileNode 入库（文件树可见、计入用户配额），视频删除时不级联清理。本地 nfo 存在时其内容即元数据唯一来源，字段残缺不补，元数据标记为不完整。

**本地媒体图片（Local Artwork）**：
视频目录下按 Jellyfin 命名的图片文件：`poster.jpg`（海报）、`fanart.jpg`（背景）、`season01-poster.jpg`（季海报）。削刮时本地存在则优先使用，仅有本地图片无 NFO 时同样走本地优先、文本字段缺失不补；削刮自 TMDB 的图片也会写回视频目录成为本地媒体图片。前端取图按记录的路径从视频目录实时读取，不复制进系统数据目录缓存。

**剧集归属（Series Attribution）**：
电视库的每个来源目录独立采用固定三层结构：`来源目录 / 剧文件夹 / 季文件夹 / 集文件`。一级子文件夹为剧，二级子文件夹为季，季文件夹下的视频为集；散在剧文件夹下的视频以及解析不出季号的季文件夹统一归入第一季（S01）；散在来源目录根下的文件和超过三层深度的文件忽略，不产生条目。剧（`t_media_series`，按用户+剧名唯一）与季（`t_media_season`）为独立层级实体，由扫描自动维护（upsert + 孤儿清理），剧集条目通过 `series_id`/`season_id` 关联；电视海报墙直接分页查询剧表。

**季/集元数据（Season/Episode Metadata）**：
TMDB 的季与集元数据。季元数据直接绑定到季（`t_media_season`），集元数据直接绑定到集条目，与剧/电影同为按用户隔离的媒体元数据。剧匹配成功后（自动削刮或手动修正），按本地实际存在的季逐个拉取整季数据（`/tv/{id}/season/{n}`）填充季与集元数据，本地没有的季不拉取。

**媒体元数据（Media Metadata）**：
影片/剧/季/集的元数据（海报、简介、评分等），**按用户隔离**，每条直接绑定到具体的电影条目、剧或季（`t_media_item`/`t_media_series`/`t_media_season`），不再全局共享；同一影片被多用户收录时各自拉取各自存储。来源标记为 `local_nfo`（本地 NFO 解析，完全信任不补全）或 `tmdb`，并带完整性状态：本地优先但字段/图片缺失时标记为不完整，不自动补全。图片按来源取本地媒体图片（从视频目录实时读取）或削刮时下载写回的 TMDB 图片，不再统一缓存到系统数据目录。TMDB API Key 与可选代理由管理员全局配置，语言 zh-CN。元数据匹配只发生在削刮（含手动修正）中，扫描不参与。自动匹配对齐 Jellyfin 策略：文件名清洗剥离季号/字幕组/质量标签后搜索，候选按标题（含 original_title）归一化比对 + 年份容差打分（`TmdbMatchScorer`），低于阈值记为未识别；匹配失败/错误的条目支持用户手动搜索修正。

**同步后扫描（Post-Sync Scan）**：
用户文件同步（用户管理中的手动同步与 cron 定时同步）或远程挂载同步完成后，自动触发该用户所有媒体库的扫描，复用现有同库扫描合并与扫描/削刮互斥机制；扫描完成后沿用现有级联逻辑自动提交一次非强制削刮。

**硬解直放（Direct Play）**：
浏览器可原生硬解的视频（如 MP4/H.264/AAC）不经转码，后端以 Range 流式直接返回文件字节的播放方式。是否可直放由后端根据 ffprobe 探测结果与浏览器能力综合判定。

**实时转码（Realtime Transcode）**：
浏览器无法硬解的视频在播放时由后端 ffmpeg 实时转码为 HLS（fMP4 切片）边转边播。硬解方式可配置：auto（自动探测，启动失败自动回退软解）/ VA-API / QSV / NVENC / 软解；显式指定的硬解方式启动失败时播放直接报错，提示用户修改配置，不自动回退。硬解设备路径与 ffmpeg 单任务线程数（-threads）可配置。全局并发转码进程数默认等于 CPU 核心数。输出质量可被播放端选择的码率档位约束；编码满足直放白名单的流可按流决策转封装直出（见转封装会话）。

**码率档位（Bitrate Tier）**：
播放端可选的输出质量档位，固定阶梯：原画 / 20M·4K / 8M·1080p / 4M·1080p / 2M·720p / 1M·480p / 500K·360p。所选档位码率低于文件实际码率时强制走实时转码，按档位的码率上限与分辨率上限输出（实际分辨率低于档位上限时不放大）；等于或高于实际码率时优先硬解直放；无法直放时按原质量转码，不受所选档位约束。档位默认「原画」，用户选择按浏览器 localStorage 记忆。

**转封装会话（Remux Session）**：
转码会话的按流决策形态：视频、音频流编码分别在直放白名单内时对该流 `-c copy` 转封装，不满足白名单的流单独转码（视频按原质量或所选码率档位，音频统一转 AAC 128k）。用于直放文件切换音轨等场景，避免全量转码。代价是 seek 与切音轨的起播点需对齐关键帧，可能产生最大一个 GOP 的偏差。

**影视设置（Media Settings）**：
系统菜单下的影视全局配置页（仅管理员），包含 TMDB 配置（API Key、HTTP 代理）与转码配置（硬解方式、设备路径、线程数），均存于系统配置表，即时生效。

**媒体详情页（Media Detail）**：
电影/电视剧点击海报后进入的详情页（Jellyfin 风格：背景横幅 backdrop + 海报 + 元信息 + 简介；电视剧含季/集列表）。未识别条目展示简版详情（文件信息 + 修正匹配入口）。支持手动刷新元数据。

**独立播放页（Standalone Player）**：
详情页点击播放后进入的全屏播放路由（无顶栏/侧栏）。复用直放/转码播放逻辑；电视剧支持页内选集与播完自动连播下一集。

**转码会话（Transcode Session）**：
一次播放对应的转码执行单元，拥有独立的 HLS 输出目录（位于系统数据目录）。空闲超时无切片请求自动终止并清理；seek 超出已转范围时重启转码进程。

**播放进度（Playback Progress）**：
按用户+媒体条目记录的播放位置，用于海报墙进度条与续播；电视剧续播自动定位到下一集。

**外部字幕（External Subtitle）**：
与视频文件同目录、主文件名前缀匹配的字幕文件（`srt`/`ass`/`ssa`/`vtt`），扫描时关联入库。播放时统一由后端 ffmpeg 转为 webvtt 供前端 `<track>` 渲染（`vtt` 原样返回，非 UTF-8 编码先探测回退 GBK），`ass`/`ssa` 特效样式不保留。远程挂载来源的字幕文件在同步后同样是本地 FileNode 镜像，播放时按需经协议适配器下载并转换缓存。

**默认字幕（Default Subtitle）**：
被标记为默认的字幕轨：内嵌轨以 ffprobe 的 default 标记为准，外部字幕以文件名中 `.default` 后缀为准（如 `Movie.zh.default.srt`）。播放时优先自动选中默认字幕；无默认字幕时按用户 localStorage 记忆的语言偏好匹配；均无则选中「无」。

## 分享领域

**分享（Share）**：
由文件所有者创建的一份公开访问凭证，同时包含“文件节点集合”与“访问控制信息”两个维度。一个分享对应一个唯一的 `shareCode`，可配置名称、描述、访问密码、有效期、最大访问次数等属性。创建后，所有者仍可增删分享项、修改访问控制、修改名称/描述、手动停用或重新启用。若用户希望同一批文件拥有不同的访问策略，需重新选择文件创建新的分享。

**分享项（Share Item）**：
分享内对单个 `FileNode` 的引用。一个分享可包含多个分享项，对应用户勾选的多文件/多文件夹。分享项只保存文件节点 ID，源文件的重命名、移动会实时反映；源文件被删除或移入回收站后，对应分享项在公开访问视图中自动过滤移除，且从回收站恢复后不会自动回到原分享。

**分享编码（Share Code）**：
分享的唯一公开标识，用于构成访问 URL `/s/{shareCode}`。长度为 8 位，字母数字混合并排除易混淆字符，数据库唯一索引。

**失效分享（Invalidated Share）**：
因手动停用、达到最大访问次数、超过有效期或被所有者删除而失去公开访问能力的分享。失效分享保留记录供所有者查看历史，可定期由后台任务清理。

**公开访问者（Public Visitor）**：
通过分享链接访问分享的未登录用户。公开访问者只能读取分享内容：浏览文件列表、预览文件、下载单个文件、下载整个分享；不能上传、修改或删除文件。分享的公开访问页以扁平列表展示顶层文件节点，点击文件夹可递归浏览其内容。

**远程分享（Remote Share）**：
分享项全部来自同一远程挂载点的分享。远程分享仅支持浏览和单个文件下载，不支持下载整个分享或打包下载。创建分享时禁止本地文件节点与远程文件节点混合。

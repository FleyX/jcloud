<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fleyx.jcloud.mapper.UserMapper">

    <select id="selectByUsernameLike" resultType="com.fleyx.jcloud.model.po.User">
        SELECT id, username, email, nickname, status, is_admin, create_time, update_time, delete_at
        FROM t_user
        WHERE delete_at = 0
          AND username LIKE CONCAT('%', #{username}, '%')
        ORDER BY create_time DESC
    </select>
</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fleyx.jcloud.mapper.PermissionResourceMapper">

    <select id="selectPermissionCodesByResourceCode" resultType="java.lang.String">
        SELECT p.code
        FROM t_permission p
                 INNER JOIN t_permission_resource pr ON pr.permission_id = p.id
                 INNER JOIN t_resource r ON r.id = pr.resource_id
        WHERE p.is_deleted = 0
          AND p.status = 1
          AND r.is_deleted = 0
          AND r.status = 1
          AND r.code = #{resourceCode}
    </select>

    <select id="selectResourceCodesByRoleIds" resultType="java.lang.String">
        SELECT DISTINCT r.code
        FROM t_resource r
                 INNER JOIN t_permission_resource pr ON pr.resource_id = r.id
                 INNER JOIN t_role_permission rp ON rp.permission_id = pr.permission_id
                 INNER JOIN t_role ro ON ro.id = rp.role_id
        WHERE r.is_deleted = 0
          AND r.status = 1
          AND ro.is_deleted = 0
          AND ro.status = 1
          AND rp.role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </select>

    <delete id="deleteByPermissionId">
        DELETE FROM t_permission_resource WHERE permission_id = #{permissionId}
    </delete>

    <insert id="batchInsert">
        INSERT INTO t_permission_resource (permission_id, resource_id, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.permissionId}, #{item.resourceId}, #{item.createTime})
        </foreach>
    </insert>

    <select id="selectResourceIdsByPermissionId" resultType="java.lang.Long">
        SELECT resource_id
        FROM t_permission_resource pr
        INNER JOIN t_resource r ON r.id = pr.resource_id
        WHERE pr.permission_id = #{permissionId}
          AND r.is_deleted = 0
          AND r.status = 1
    </select>

    <select id="selectResourceCodesByPermissionIds" resultType="java.lang.String">
        SELECT DISTINCT r.code
        FROM t_resource r
        INNER JOIN t_permission_resource pr ON pr.resource_id = r.id
        WHERE r.is_deleted = 0
          AND r.status = 1
          AND pr.permission_id IN
        <foreach collection="permissionIds" item="pid" open="(" separator="," close=")">
            #{pid}
        </foreach>
    </select>
</mapper>

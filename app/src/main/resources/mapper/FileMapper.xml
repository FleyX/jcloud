<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fleyx.jcloud.mapper.FileMapper">

    <!-- 物理删除指定 ID 的文件节点 -->
    <delete id="physicalDeleteById">
        DELETE FROM t_file_node WHERE id = #{id}
    </delete>

    <!-- 批量物理删除文件节点 -->
    <delete id="physicalDeleteByIds">
        DELETE FROM t_file_node WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 查询指定完整 id 路径前缀下的所有文件节点（含自身） -->
    <select id="selectByIdPathPrefix" resultType="com.fleyx.jcloud.model.po.FileNode">
        SELECT *
        FROM t_file_node
        WHERE user_id = #{userId}
          AND (
              (path = #{path} AND id = #{id})
              OR path = CONCAT(#{path}, '.', #{id})
              OR path LIKE CONCAT(#{path}, '.', #{id}, '.%')
          )
        ORDER BY path
    </select>

    <!-- 查询指定完整 id 路径前缀下的所有文件类型节点 -->
    <select id="selectFilesByIdPathPrefix" resultType="com.fleyx.jcloud.model.po.FileNode">
        SELECT *
        FROM t_file_node
        WHERE user_id = #{userId}
          AND type = 'file'
          AND (
              (path = #{path} AND id = #{id})
              OR path = CONCAT(#{path}, '.', #{id})
              OR path LIKE CONCAT(#{path}, '.', #{id}, '.%')
          )
        ORDER BY path
    </select>

    <!-- 按名称关键字模糊搜索用户的文件节点（跨文件夹） -->
    <select id="searchByName" resultType="com.fleyx.jcloud.model.po.FileNode">
        SELECT *
        FROM t_file_node
        WHERE user_id = #{userId}
          AND (name % #{keyword} OR name ILIKE CONCAT('%', #{likePattern}, '%') ESCAPE '\')
        ORDER BY type DESC, create_time DESC
    </select>

    <!-- 根据用户 ID 和父节点 ID 查询直接子节点 -->
    <select id="selectByParentId" resultType="com.fleyx.jcloud.model.po.FileNode">
        SELECT *
        FROM t_file_node
        WHERE user_id = #{userId}
          AND parent_id = #{parentId}
        ORDER BY type DESC, create_time DESC
    </select>

</mapper>
